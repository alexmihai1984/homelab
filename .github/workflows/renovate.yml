name: Renovate

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - uses: renovatebot/github-action@v46.1.14
        with:
          token: ${{ secrets.GITHUB_TOKEN }}